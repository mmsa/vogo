{
  "name": "vogoplus-app-webext",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "zip": "cd dist && zip -r ../vogoplus-app_ext.zip . -x '*.DS_Store' '*.git*'",
    "package": "npm run build && ./build.sh && npm run zip",
    "publish:prepare": "npm run package && echo '✅ Extension packaged! Ready to upload: vogoplus-app_ext.zip'"
  },
  "devDependencies": {
    "vite": "^5",
    "typescript": "^5",
    "@types/chrome": "^0.0.270"
  }
}

